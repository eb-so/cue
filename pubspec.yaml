name: cue
description: "Physics-first Flutter animations with a composable, timeline-driven API"
version: 0.3.1
homepage: https://github.com/Milad-Akarie/cue
topics: 
  - animation
  - motion
  - timeline
  - keyframes
  - effects

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  gooey:
    git:
      url: https://github.com/eb-so/gooey.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  coverage: ^1.15.0 
  flutter_lints: ^6.0.0
 
